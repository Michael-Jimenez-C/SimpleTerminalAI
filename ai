#!/usr/bin/python3.11
import google.generativeai as genai
import os
import sys
import json
from colors import light_blue, light_red, red
from components import Parser


args = sys.argv[1:]

if len(args) == 0:
    print(red('No puede mandar una consulta vacia'))
    sys.exit(1)


parser = Parser()
parser.parse(list(args))

print(parser.argsr)

genai.configure(api_key='AIzaSyD24CrV9-W7BHJvQItoGP5TEYJLXpM861s')#os.environ["API_KEY"])


#model = genai.GenerativeModel('gemini-1.5-flash')

#promp = ""
#for i in sys.argv[1:]:
#    promp+=i+' '

#response = model.generate_content('Rol: eres un asistente personal, y explicas de forma breve en 2 o 3 parrafos \nuser:'+promp)

#print(light_blue('Usuarios:'),promp)
#print(light_red('Gemini:'),response.text)

